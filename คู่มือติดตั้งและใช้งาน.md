# คู่มือติดตั้งและใช้งาน — ระบบติดตาม KPI (DHDC)

> ระบบติดตามตัวชี้วัด HDC กระทรวงสาธารณสุข  
> พัฒนาด้วย Next.js 14 + MariaDB + MOPH Open Data API

> ⚠️ **อัปเดต 2026-06 — อ่านก่อน:** เอกสารหลักที่ทันสมัยคือ [README.md](README.md) · [CLAUDE.md](CLAUDE.md) · [docs/production-runbook.md](docs/production-runbook.md)
> คู่มือนี้เป็น**รายละเอียดติดตั้ง/ops** (Nginx, systemd, troubleshooting) ที่เอกสารหลักไม่มี แต่มี 2 จุดที่ล้าสมัย:
> 1) บางจุดเขียน port **3000** — จริงคือ **3002**
> 2) ตัวอย่าง `lib/db.ts` แบบ hardcode `host:'192.168.0.236'` **เลิกใช้แล้ว** — ปัจจุบัน `db.ts` อ่านจาก **env** (default dev = `dhdc_dev`) · **ห้าม hardcode production** ตั้งผ่าน `.env.local` แทน (ดู runbook)

---

## สารบัญ

1. [ภาพรวมระบบ](#1-ภาพรวมระบบ)
2. [ความต้องการระบบ](#2-ความต้องการระบบ)
3. [การติดตั้งบน Windows](#3-การติดตั้งบน-windows)
4. [การติดตั้งบน Linux (Ubuntu/Debian)](#4-การติดตั้งบน-linux-ubuntudebian)
5. [การตั้งค่าฐานข้อมูล MariaDB](#5-การตั้งค่าฐานข้อมูล-mariadb)
6. [การตั้งค่าแอปพลิเคชัน](#6-การตั้งค่าแอปพลิเคชัน)
7. [การเริ่มใช้งานครั้งแรก](#7-การเริ่มใช้งานครั้งแรก)
8. [คู่มือการใช้งานโดยละเอียด](#8-คู่มือการใช้งานโดยละเอียด)
9. [การตั้งค่า Auto Batch (Cron)](#9-การตั้งค่า-auto-batch-cron)
10. [การแก้ไขปัญหาเบื้องต้น](#10-การแก้ไขปัญหาเบื้องต้น)

---

## 1. ภาพรวมระบบ

```
┌─────────────────────────────────────────────────────┐
│                  DHDC KPI System                    │
│                                                     │
│  Next.js 14 (App Router)  ←→  MariaDB 10.x+        │
│                                                     │
│  หน้าจอ:                     ตาราง DB:             │
│  • /login      — เข้าสู่ระบบ  • users               │
│  • /dashboard  — ภาพรวม KPI   • kpi_reports         │
│  • /kpi/[id]   — รายละเอียด   • monthly_data        │
│  • /compare    — เปรียบเทียบ  • moph_report_catalog │
│  • /admin      — จัดการระบบ   • moph_snapshot       │
│                                                     │
│  MOPH Open Data API (opendata.moph.go.th)           │
│  → ดึงข้อมูลอัตโนมัติทุกวันจันทร์ 07:00            │
└─────────────────────────────────────────────────────┘
```

**บัญชีผู้ใช้เริ่มต้น (seed data):**

| อีเมล | รหัสผ่าน | สิทธิ์ |
|---|---|---|
| admin@hospital.go.th | admin123 | Admin |
| staff@hospital.go.th | staff123 | Staff |
| staff2@hospital.go.th | staff123 | Staff |

---

## 2. ความต้องการระบบ

| รายการ | เวอร์ชันขั้นต่ำ | แนะนำ |
|---|---|---|
| Node.js | 18.x | 20.x LTS |
| npm | 9.x | 10.x |
| MariaDB | 10.6 | 10.11 LTS |
| RAM | 1 GB | 2 GB+ |
| พื้นที่ดิสก์ | 500 MB | 1 GB+ |
| OS | Windows 10 / Ubuntu 20.04 | Windows 11 / Ubuntu 22.04 |

---

## 3. การติดตั้งบน Windows

### 3.1 ติดตั้ง Node.js

1. ดาวน์โหลด Node.js จาก https://nodejs.org เลือก **LTS**
2. รันตัวติดตั้ง `.msi` → คลิก Next ทุกขั้นตอน ✓ ติ๊ก "Add to PATH"
3. ตรวจสอบหลังติดตั้ง:

```cmd
node -v
npm -v
```

ควรแสดง `v20.x.x` และ `10.x.x` (หรือสูงกว่า)

---

### 3.2 ติดตั้ง MariaDB (บนเครื่องเดียวกัน หรือเครื่อง DB แยก)

> **หากใช้ MariaDB เครื่องอื่น (เช่น 192.168.0.236) ข้ามขั้นตอนนี้ได้**

1. ดาวน์โหลด MariaDB จาก https://mariadb.org/download/
2. รันตัวติดตั้ง → ตั้งรหัสผ่าน root → ✓ ติ๊ก "Install as service"
3. ตรวจสอบ:

```cmd
mysql -u root -p
```

---

### 3.3 ดาวน์โหลดและติดตั้งโปรเจกต์

**วิธีที่ 1 — Clone จาก Git (แนะนำ):**
```cmd
git clone <repository-url> C:\DHDC
cd C:\DHDC
npm install
```

**วิธีที่ 2 — คัดลอกโฟลเดอร์โปรเจกต์:**
```cmd
cd C:\DHDC
npm install
```

> ขั้นตอน `npm install` ใช้เวลาประมาณ 1-3 นาที (ดาวน์โหลด dependencies ~200 MB)

---

### 3.4 ตั้งค่าการเชื่อมต่อฐานข้อมูล

เปิดไฟล์ `lib\db.ts` แก้ค่าตามเครื่อง DB ของคุณ:

```typescript
const pool = mysql.createPool({
  host: '192.168.0.236',   // ← IP เครื่อง MariaDB
  port: 3306,
  user: 'dong',            // ← ชื่อผู้ใช้ DB
  password: 'pass27980',   // ← รหัสผ่าน DB
  database: 'dhdc',        // ← ชื่อฐานข้อมูล
  ...
})
```

---

### 3.5 เริ่มใช้งานบน Windows

**โหมด Development (แนะนำสำหรับทดสอบ):**
```cmd
npm run dev
```

**โหมด Production:**
```cmd
npm run build
npm run start
```

เปิดเบราว์เซอร์ไปที่ http://localhost:3000

---

### 3.6 รันเป็น Background Service บน Windows (ทางเลือก)

ใช้ `pm2` เพื่อให้รันตลอดเวลาแม้ปิด CMD:

```cmd
npm install -g pm2
npm run build
pm2 start "npm run start" --name dhdc-kpi
pm2 save
pm2 startup
```

ตรวจสอบสถานะ:
```cmd
pm2 status
pm2 logs dhdc-kpi
```

---

## 4. การติดตั้งบน Linux (Ubuntu/Debian)

### 4.1 อัปเดตระบบและติดตั้ง Node.js

```bash
# อัปเดต package list
sudo apt update && sudo apt upgrade -y

# ติดตั้ง Node.js 20.x LTS ผ่าน NodeSource
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# ตรวจสอบ
node -v   # v20.x.x
npm -v    # 10.x.x
```

---

### 4.2 ติดตั้ง MariaDB (บนเครื่องเดียวกัน หรือเครื่อง DB แยก)

> **หากใช้ MariaDB เครื่องอื่น ข้ามขั้นตอนนี้ได้**

```bash
sudo apt install -y mariadb-server mariadb-client

# เริ่ม service และตั้งให้เริ่มอัตโนมัติ
sudo systemctl start mariadb
sudo systemctl enable mariadb

# ตั้งค่าความปลอดภัยเบื้องต้น (แนะนำ)
sudo mysql_secure_installation
```

---

### 4.3 ดาวน์โหลดและติดตั้งโปรเจกต์

```bash
# ตัวอย่าง: วางโปรเจกต์ที่ /opt/dhdc
sudo mkdir -p /opt/dhdc
sudo chown $USER:$USER /opt/dhdc

# Clone หรือคัดลอกไฟล์
git clone <repository-url> /opt/dhdc
cd /opt/dhdc

# ติดตั้ง dependencies
npm install
```

---

### 4.4 ตั้งค่าการเชื่อมต่อฐานข้อมูล

```bash
nano /opt/dhdc/lib/db.ts
```

แก้ไขค่า:
```typescript
const pool = mysql.createPool({
  host: '192.168.0.236',   // ← IP เครื่อง MariaDB
  port: 3306,
  user: 'dong',            // ← ชื่อผู้ใช้ DB
  password: 'pass27980',   // ← รหัสผ่าน DB
  database: 'dhdc',        // ← ชื่อฐานข้อมูล
  ...
})
```

---

### 4.5 เริ่มใช้งานบน Linux

**โหมด Development:**
```bash
cd /opt/dhdc
npm run dev
```

**โหมด Production:**
```bash
npm run build
npm run start
```

---

### 4.6 ตั้งค่า Systemd Service (แนะนำสำหรับ Production)

สร้างไฟล์ service:
```bash
sudo nano /etc/systemd/system/dhdc-kpi.service
```

วางเนื้อหา:
```ini
[Unit]
Description=DHDC KPI System (Next.js)
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/dhdc
ExecStart=/usr/bin/npm run start
Restart=on-failure
RestartSec=5
Environment=NODE_ENV=production
Environment=PORT=3000

[Install]
WantedBy=multi-user.target
```

เปิดใช้งาน:
```bash
sudo systemctl daemon-reload
sudo systemctl enable dhdc-kpi
sudo systemctl start dhdc-kpi

# ตรวจสอบสถานะ
sudo systemctl status dhdc-kpi

# ดู log แบบ real-time
sudo journalctl -u dhdc-kpi -f
```

---

### 4.7 ตั้งค่า Nginx Reverse Proxy (ทางเลือก)

```bash
sudo apt install -y nginx
sudo nano /etc/nginx/sites-available/dhdc
```

```nginx
server {
    listen 80;
    server_name your-server-ip-or-domain;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

```bash
sudo ln -s /etc/nginx/sites-available/dhdc /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

---

## 5. การตั้งค่าฐานข้อมูล MariaDB

### 5.1 สร้างฐานข้อมูลและผู้ใช้

เข้า MariaDB ด้วยสิทธิ์ root:

**Windows:**
```cmd
mysql -u root -p
```

**Linux:**
```bash
sudo mysql
```

รันคำสั่ง SQL:
```sql
-- สร้างฐานข้อมูล
CREATE DATABASE IF NOT EXISTS dhdc
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

-- สร้างผู้ใช้และให้สิทธิ์
-- (เปลี่ยน 'pass27980' เป็นรหัสผ่านที่ต้องการ)
CREATE USER IF NOT EXISTS 'dong'@'%' IDENTIFIED BY 'pass27980';
GRANT ALL PRIVILEGES ON dhdc.* TO 'dong'@'%';
FLUSH PRIVILEGES;

-- ตรวจสอบ
SHOW DATABASES;
```

### 5.2 อนุญาตการเชื่อมต่อจากภายนอก (กรณีเครื่อง DB แยก)

**Linux — แก้ไข bind-address:**
```bash
sudo nano /etc/mysql/mariadb.conf.d/50-server.cnf
```
เปลี่ยน:
```ini
# bind-address = 127.0.0.1   ← comment หรือเปลี่ยน
bind-address = 0.0.0.0
```
```bash
sudo systemctl restart mariadb
```

**Windows — แก้ไขผ่าน Configurator หรือ my.ini:**
```ini
[mysqld]
bind-address=0.0.0.0
```

**Firewall (Linux):**
```bash
sudo ufw allow 3306/tcp
sudo ufw reload
```

---

## 6. การตั้งค่าแอปพลิเคชัน

### 6.1 ตัวแปร Environment (ทางเลือก แต่แนะนำ)

สร้างไฟล์ `.env.local` ที่ root โปรเจกต์:

```bash
# Windows:
notepad E:\DHDC\.env.local

# Linux:
nano /opt/dhdc/.env.local
```

เนื้อหา `.env.local`:
```env
# ─── Port ────────────────────────────────────────────
PORT=3000

# ─── MOPH Auto Batch (Cron) ──────────────────────────
# ตาราง cron (default: ทุกวันจันทร์ 07:00)
MOPH_CRON=0 7 * * 1

# timezone
MOPH_CRON_TZ=Asia/Bangkok

# ปีงบประมาณ (default: คำนวณอัตโนมัติ)
# MOPH_FETCH_YEAR=2569

# จังหวัด (รหัส 2 หลัก)
MOPH_FETCH_PROVINCE=66

# areacode prefix (จังหวัด+อำเภอ) เช่น 6611 = จ.พิจิตร อ.เมือง
MOPH_FETCH_AREACODE=6611

# hospcode (ถ้าต้องการเฉพาะโรงพยาบาลเดียว)
# MOPH_FETCH_HOSPCODE=27980

# ปิด cron (ใส่ 1 เพื่อปิด)
# MOPH_CRON_DISABLED=1
```

### 6.2 เปลี่ยน Port (ทางเลือก)

**Windows:**
```cmd
set PORT=3055 && npm run start
```

**Linux:**
```bash
PORT=3055 npm run start
```

หรือใน `package.json`:
```json
"start": "next start -p 3055"
```

---

## 7. การเริ่มใช้งานครั้งแรก

### 7.1 Migrate & Seed ฐานข้อมูล

1. เปิดเบราว์เซอร์ไปที่ http://localhost:3000
2. ล็อกอินด้วย `admin@hospital.go.th` / `admin123`
3. ไปที่เมนู **จัดการระบบ (Admin)**
4. คลิกแท็บ **🗄️ Database**
5. คลิกปุ่ม **🚀 Migrate & Seed**

ระบบจะ:
- สร้างตารางทั้งหมด (users, kpi_reports, monthly_data, moph_report_catalog, moph_snapshot)
- ใส่ข้อมูลเริ่มต้น (3 users, 1 KPI, 6 เดือน monthly data)

6. ตรวจสอบผลโดยคลิก **ตรวจสอบการเชื่อมต่อ** — ควรเห็นจำนวนข้อมูลในแต่ละตาราง

---

## 8. คู่มือการใช้งานโดยละเอียด

### 8.1 หน้าเข้าสู่ระบบ (`/login`)

- กรอก **อีเมล** และ **รหัสผ่าน**
- คลิก **เข้าสู่ระบบ**
- ระบบบันทึก session ใน localStorage (ไม่มี timeout อัตโนมัติ)
- **Admin** จะเห็นลิงก์ "จัดการระบบ" ในเมนูบาร์ (Staff จะไม่เห็น)

---

### 8.2 Dashboard (`/dashboard`)

ภาพรวมตัวชี้วัดทั้งหมด

**การ์ด KPI แต่ละตัวแสดง:**
- ชื่อตัวชี้วัดและหมวดหมู่
- ค่าล่าสุด vs เป้าหมาย (แถบความก้าวหน้า)
- สถานะ: `กำลังดำเนินการ` / `เสร็จแล้ว` / `เกินเวลา`
- กราฟ Mini Sparkline แนวโน้มรายเดือน
- ผู้รับผิดชอบและกำหนดเสร็จ

**ฟิลเตอร์:**
- กรองตาม **หมวดหมู่** (NCD, แม่และเด็ก, โรคติดต่อ ฯลฯ)
- กรองตาม **สถานะ**
- **ค้นหา** ชื่อ KPI

---

### 8.3 รายละเอียด KPI (`/kpi/[id]`)

คลิกการ์ด KPI ใดก็ได้จาก Dashboard

**แสดงข้อมูล:**
- กราฟเส้นรายเดือน (ค่าจริง vs เป้าหมาย)
- ตารางข้อมูลรายเดือนทั้งหมด
- ข้อมูลทั่วไป: ผู้รับผิดชอบ, หน่วย, กำหนดเสร็จ, คำอธิบาย

---

### 8.4 หน้าเปรียบเทียบ (`/compare`)

เปรียบเทียบ KPI หลายตัวพร้อมกันในกราฟเดียว

1. เลือก KPI ที่ต้องการเปรียบเทียบ (เลือกได้หลายตัว)
2. กราฟจะแสดงเส้นทุก KPI ที่เลือกในแกนเดียวกัน

---

### 8.5 หน้าจัดการระบบ (`/admin`) — เฉพาะ Admin

เข้าได้จากเมนูบาร์หรือ http://localhost:3000/admin

---

#### แท็บ 📋 KPI

**เพิ่ม KPI ใหม่:**
1. คลิก **+ เพิ่ม KPI ใหม่**
2. กรอกข้อมูลในฟอร์ม:
   - ชื่อ KPI (ภาษาไทย)
   - หมวดหมู่ (NCD / แม่และเด็ก / โรคติดต่อ / ผู้สูงอายุ / สุขภาพจิต / อื่นๆ)
   - ผู้รับผิดชอบ
   - เป้าหมาย (ตัวเลข) และหน่วย (เช่น %)
   - กำหนดเสร็จ
   - **MOPH API Config** (ถ้าต้องการดึงอัตโนมัติ):
     - Table Name เช่น `s_dm_control`
     - Value Field เช่น `hba1c`, `result`
     - Target Field เช่น `target`
     - วิธีคำนวณ: ร้อยละ / ผลรวม / ค่าดิบ
3. คลิก **เพิ่ม KPI**

**แก้ไข KPI:** คลิกปุ่ม **แก้ไข** ในแถวนั้น

**ลบ KPI:** คลิกปุ่ม **ลบ** → ยืนยัน (จะลบข้อมูลรายเดือนที่เกี่ยวข้องด้วย)

**เปลี่ยนสถานะ:** คลิก dropdown สถานะในแถวได้เลย (บันทึกทันที)

**ไปหน้า MOPH:** คลิก 🌐 ในแถว KPI เพื่อข้ามไปแท็บ MOPH พร้อม auto-fill ค่า

---

#### แท็บ 👥 ผู้ใช้

**เพิ่มผู้ใช้ใหม่:**
1. คลิก **+ เพิ่มผู้ใช้**
2. กรอก:
   - อีเมล (ต้องไม่ซ้ำในระบบ)
   - ชื่อ-สกุล
   - รหัสผ่าน
   - สิทธิ์: Staff หรือ Admin
   - หน่วยงาน (เช่น งาน NCD)
3. คลิก **เพิ่มผู้ใช้**

**เปลี่ยนรหัสผ่าน:**
1. คลิกปุ่ม **🔑 รหัสผ่าน** ในแถวผู้ใช้ที่ต้องการ
2. กรอกรหัสผ่านใหม่
3. คลิก **บันทึก** หรือกด **Enter**

**ลบผู้ใช้:**
1. คลิปปุ่ม **ลบ** ในแถวนั้น
2. ยืนยัน → ผู้ใช้จะถูกลบออกจากระบบ

> ⚠️ **ข้อควรระวัง:** อย่าลบ Admin คนสุดท้าย ระบบจะไม่สามารถเข้าจัดการได้

---

#### แท็บ 🌐 ดึงข้อมูล MOPH

ดึงข้อมูลจาก MOPH Open Data API แบบ Manual

**ขั้นตอนการใช้งาน:**

**ส่วนที่ 1 — ตั้งค่า Config:**

| ช่อง | คำอธิบาย | ตัวอย่าง |
|---|---|---|
| KPI | เลือก KPI เพื่อ auto-fill ค่า Config | ผู้ป่วยเบาหวาน... |
| Table Name | ชื่อตารางใน MOPH | `s_dm_control` |
| ปีงบประมาณ | พ.ศ. (ปีงบ = ต.ค.-ก.ย.) | 2568 |
| จังหวัด | รหัสจังหวัด 2 หลัก | 66 = พิจิตร |
| Hospcode | รหัสโรงพยาบาล (ถ้าต้องการเฉพาะ รพ.) | 27980 |
| Areacode prefix | รหัสเขต (จ.+อ.) | 6611 |

**ส่วนที่ 2 — Field Config (ล็อคต่อ KPI):**

| ช่อง | คำอธิบาย |
|---|---|
| Value Field | ชื่อ column ที่เป็นค่าผลลัพธ์ เช่น `hba1c`, `result` |
| Target Field | ชื่อ column ที่เป็นเป้าหมาย เช่น `target` |
| วิธีคำนวณ | `ร้อยละ` = (sum valueField / sum targetField) × 100 |

- **🔒 สัญลักษณ์สีเขียว** = ค่าถูกบันทึกแล้ว
- **● สัญลักษณ์สีส้ม** = มีการเปลี่ยนแปลงที่ยังไม่บันทึก
- คลิกปุ่ม **🔒 ล็อค Field** เพื่อบันทึกค่าลง DB

**ส่วนที่ 3 — ดูตัวอย่าง:**
- คลิก **🔍 ดูตัวอย่าง** เพื่อดึงข้อมูลตัวอย่าง (ไม่บันทึก)
- แสดง: จำนวน rows, ฟิลด์ที่มีใน API, ค่าคำนวณเบื้องต้น

**ส่วนที่ 4 — บันทึก:**

**บันทึกทีละ KPI:**
1. เลือก KPI ที่ต้องการบันทึกใน dropdown "บันทึกสำหรับ KPI"
2. เลือกเดือนที่จะบันทึก
3. คลิก **💾 บันทึก**

**Batch Save — บันทึกทุก KPI พร้อมกัน:**
1. ตั้งค่าเดือนที่บันทึก
2. คลิก **🚀 Batch Save ทุก KPI**
3. ระบบจะอ่าน Field Config ที่ล็อคไว้ของแต่ละ KPI → ดึงข้อมูล → บันทึก monthly_data
4. ผลลัพธ์แสดงเป็นตาราง: KPI, Rows, ค่าที่คำนวณได้, สถานะ
5. คลิก 🗑️ ในแถวผลลัพธ์เพื่อลบข้อมูลนั้นออก (ถ้าข้อมูลผิด)

---

#### แท็บ 📊 Catalog

คลังรายงาน MOPH — ลงทะเบียนรายงานล่วงหน้า แล้วดึงทีเดียวด้วยปุ่ม Snapshot

**เพิ่มรายงานใหม่:**
1. คลิก **+ เพิ่มรายงาน**
2. กรอก:
   - รหัสรายงาน (ID) — ได้จาก MOPH portal หรือตั้งเอง
   - ชื่อรายงาน
   - MOPH Table Name
   - Value/Target Field
   - วิธีคำนวณ
   - Areacode หรือ Hospcode (ถ้าต้องการ)
3. คลิก **บันทึก**

**Snapshot ทั้ง Catalog:**
1. เลือกปีงบประมาณ
2. เลือกเดือน
3. คลิก **🚀 Snapshot ทั้งหมด**
4. ระบบดึงข้อมูลทุกรายงานใน catalog → เก็บใน `moph_snapshot`

---

#### แท็บ 🗄️ Database

- **ตรวจสอบการเชื่อมต่อ** — ping MariaDB พร้อมแสดงจำนวน records แต่ละตาราง
- **🚀 Migrate & Seed** — สร้างตาราง + ใส่ข้อมูลเริ่มต้น (ปลอดภัย รันซ้ำได้)

---

## 9. การตั้งค่า Auto Batch (Cron)

ระบบดึงข้อมูล MOPH อัตโนมัติผ่าน `node-cron` ที่รันอยู่ในโปรเซส Next.js

**ค่าเริ่มต้น:** ทุกวันจันทร์ เวลา 07:00 (Asia/Bangkok)

### 9.1 ปรับตาราง Cron

ผ่าน `.env.local`:
```env
# ทุกวัน 06:00
MOPH_CRON=0 6 * * *

# ทุกวันจันทร์และพฤหัสบดี 07:30
MOPH_CRON=30 7 * * 1,4

# ทุกวันที่ 1 ของเดือน 08:00
MOPH_CRON=0 8 1 * *
```

**ตารางรูปแบบ Cron:**
```
┌──── นาที   (0-59)
│ ┌─── ชั่วโมง  (0-23)
│ │ ┌── วันที่   (1-31)
│ │ │ ┌─ เดือน    (1-12)
│ │ │ │ ┌ วันสัปดาห์ (0-7, 0=อาทิตย์)
│ │ │ │ │
0 7 * * 1   ← วันจันทร์ 07:00
```

### 9.2 ปิด Cron ชั่วคราว

```env
MOPH_CRON_DISABLED=1
```

### 9.3 ตรวจสอบว่า Cron ทำงาน

ดู log ของ server:
```
[cron] ตั้งเวลา MOPH auto-batch แล้ว: "0 7 * * 1" (Asia/Bangkok)
```

เมื่อ Cron ทำงาน:
```
[cron] เริ่มดึง MOPH อัตโนมัติ @ 2025-07-07T00:00:00.000Z
[cron] เสร็จสิ้น: เดือน 2025-07 | สำเร็จ 3/3 | ล้มเหลว 0
```

---

## 10. การแก้ไขปัญหาเบื้องต้น

### ปัญหา: เชื่อมต่อ DB ไม่ได้ — `ECONNREFUSED`

**ตรวจสอบ:**
```bash
# ทดสอบเชื่อมต่อ DB โดยตรง
mysql -h 192.168.0.236 -u dong -p dhdc

# ตรวจ MariaDB รัน
# Linux:
sudo systemctl status mariadb
# Windows:
sc query mysql
```

**สาเหตุที่พบบ่อย:**
- ไฟล์ `lib/db.ts` มี host/user/password ผิด
- MariaDB ไม่ได้รัน
- Firewall บล็อก port 3306

---

### ปัญหา: `EADDRINUSE: address already in use :::3000`

Port ถูกใช้งานอยู่แล้ว

**Windows:**
```cmd
# หา PID ที่ใช้ port 3000
netstat -ano | findstr :3000
# ปิด process นั้น
taskkill /PID <PID> /F
```

**Linux:**
```bash
sudo lsof -i :3000
sudo kill -9 <PID>
```

หรือเปลี่ยน port:
```bash
PORT=3055 npm run start
```

---

### ปัญหา: หน้าแสดง 500 หลัง Migrate & Seed

**สาเหตุ:** ตารางในฐานข้อมูลอาจมีโครงสร้างเก่า

**แก้ไข:** รัน Migrate & Seed อีกครั้ง (ระบบใช้ `IF NOT EXISTS` และ `INSERT IGNORE` ปลอดภัย)

---

### ปัญหา: MOPH API ไม่มีข้อมูล / rows = 0

**ตรวจสอบ:**
1. Table Name ถูกต้อง? ทดสอบผ่านแท็บ MOPH → **ดูตัวอย่าง**
2. ปีงบประมาณถูกต้อง? (ปีงบ ≠ ปีปฏิทิน — ต.ค. 67 = ปีงบ 2568)
3. Areacode/Province ถูกต้อง?
4. ตรวจสอบ internet connection (เชื่อมต่อ opendata.moph.go.th ได้ไหม)

---

### ปัญหา: ล็อกอินไม่ผ่าน

1. ตรวจสอบว่า Migrate & Seed แล้วหรือยัง
2. ใช้ email/password ต้นฉบับ: `admin@hospital.go.th` / `admin123`
3. ตรวจสอบ DB ว่ามีข้อมูลในตาราง users:
```sql
SELECT email, role FROM users;
```

---

### ปัญหา: ข้อมูล Dashboard ไม่อัปเดตหลัง Batch Save

กด **F5** หรือ reload หน้าเพื่อดึงข้อมูลใหม่จาก DB

---

## สรุปโครงสร้างโปรเจกต์

```
DHDC/
├── app/
│   ├── admin/page.tsx          ← หน้าจัดการระบบ (admin เท่านั้น)
│   ├── dashboard/page.tsx      ← ภาพรวม KPI
│   ├── kpi/[id]/page.tsx       ← รายละเอียด KPI
│   ├── compare/page.tsx        ← เปรียบเทียบ KPI
│   ├── login/page.tsx          ← เข้าสู่ระบบ
│   └── api/
│       ├── auth/login/         ← POST เข้าสู่ระบบ
│       ├── kpis/               ← GET, POST + [id]: PUT, DELETE, PATCH
│       ├── users/              ← GET, POST + [id]: DELETE, PATCH
│       ├── monthly/            ← GET, POST, DELETE
│       ├── moph/               ← GET preview, POST save, batch, catalog, snapshot
│       └── init/               ← GET สถานะ DB, POST migrate+seed
├── lib/
│   ├── db.ts                   ← MySQL connection pool
│   ├── types.ts                ← TypeScript interfaces
│   ├── initialData.ts          ← ข้อมูล seed เริ่มต้น
│   ├── mophBatch.ts            ← Logic ดึงข้อมูล MOPH (shared)
│   └── scheduler.ts            ← node-cron auto batch
├── instrumentation.ts          ← เริ่ม cron เมื่อ server boot
├── next.config.mjs             ← instrumentationHook: true
└── .env.local                  ← ตัวแปร environment (สร้างเอง)
```

---

*คู่มือฉบับนี้ครอบคลุมการติดตั้งและใช้งานระบบ DHDC KPI เวอร์ชัน 0.1.0*  
*อัปเดตล่าสุด: พฤษภาคม 2568*
